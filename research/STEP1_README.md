# Step 1 isolated research

Step 1 timing validation is developed and tested independently from Main release CI.

The workflow is intended to be run manually or from Step 1 feature/research branches. Only after the timing contract is proven should a small regression test be promoted to Main CI.