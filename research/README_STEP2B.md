# Step 2b research

This directory is intentionally kept separate from the stable release CI. The historical A-B-C benchmark is run from its dedicated workflow after the benchmark implementation and deterministic tests are validated on the research branch.
